The SINU action button — navy primary, outlined secondary, ghost, and danger; hover darkens, press scales 0.98.
```jsx
<Button>Apply now</Button>
<Button variant="secondary" size="sm">View courses</Button>
```
Primary = one per view. Gold is never a button color.