# URL of the Wagtail GraphQL endpoint. Read on the server only — the
# browser client posts to /api/cms which proxies to this URL, so no CMS
# host is ever exposed in the client bundle.
CMS_GRAPHQL_URL=http://127.0.0.1:8000/graphql/
